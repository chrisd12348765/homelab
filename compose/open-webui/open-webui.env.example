# Copy to .env and fill (or decrypt open-webui.enc.env at deploy time).
# The local llama-swap stack (STT/TTS/RAG/task model too) needs no real key;
# dummy placeholders live directly in docker-compose.yml.
OPEN_WEBUI_PORT=3000
WEBUI_DOCKER_TAG=main
WEBUI_SECRET_KEY=changeme-long-random-string
